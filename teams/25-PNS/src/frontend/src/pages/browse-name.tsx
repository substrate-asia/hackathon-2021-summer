import React, { FC } from "react";
import { css } from "@emotion/css";

let PageBrowseName: FC<{ className?: string }> = React.memo((props) => {
  /** Plugins */
  /** Methods */
  /** Effects */
  /** Renderers */
  return <div className={props.className}>Browse name of others</div>;
});

export default PageBrowseName;
